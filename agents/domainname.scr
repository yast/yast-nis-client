/**
 * File:
 *   domainname.scr
 * Summary:
 *   SCR Agent for accessing /etc/defaultdomain
 * Access:
 *   r/w
 * Authors:
 *   Martin Vidner <mvidner@suse.cz>
 * See:
 *   anyagent
 *   libscr
 * Example:
 *   Read(.etc.defaultdomain)
 * ("My-NIS-domain")
 *
 * $Id$
 *
 * Returns the domain name for NIS (and some other services)
 */

.etc.defaultdomain

`ag_anyagent(
    Description (
	``(File ("/etc/defaultdomain")),
	"\n",			// Characters starting a comment line
	false,			// Read-only?
	``(String ("^ \t\n"))	// just a string
	)
    )
